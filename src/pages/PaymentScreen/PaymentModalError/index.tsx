import React from 'react';

import { Container, ContainerModal, ContainerModalContent } from './styles';

const PaymentModalError: React.FC = () => (
  <ContainerModal>
    <Container>
      <ContainerModalContent />
    </Container>
  </ContainerModal>
);

export default PaymentModalError;
